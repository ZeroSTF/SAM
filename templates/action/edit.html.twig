{% extends 'template.html.twig' %}

{% block body %}
    <div class="card col-md-12">
        <div class="card-header">
            <h3 class="card-title">Modifier une action</h3>
        </div>
        <div class="card-body">
            {% set show_lines = false %}
            {{ include('action/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>
        <div class="card-footer">
            <a href="{{ path('app_action_index', {'idb': idb}) }}" class="btn btn-default">Retour à la liste</a>
            {{ include('action/_delete_form.html.twig', {'idb': idb}) }}
        </div>
    </div>
{% endblock %}
