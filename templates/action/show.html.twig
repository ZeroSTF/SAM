{% extends 'template.html.twig' %}

{% block body %}
    <h1>Détails de l'action</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Description :</th>
            <td>{{ action.description }}</td>
        </tr>
        <tr>
            <th>Indicateur temporel :</th>
            <td> {{ action.iT }} Jours </td>
        </tr>
        <tr>
            <th>Indicateur en Km :</th>
            <td> {{ action.iKm }} Km </td>
        </tr>
        </tbody>
    </table>
    <a href="{{ path('app_action_index', {'idb': idb}) }}" class="btn btn-outline-secondary mt-3"><i class="fas fa-arrow-left"></i>
        Retour à la liste</a>


    {{ include('action/_delete_form.html.twig', {'idb': idb}) }}

{% endblock %}
