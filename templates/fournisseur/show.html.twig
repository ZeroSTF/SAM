{% extends 'template.html.twig' %}

{% block body %}
    <h1>Détails du fournisseur</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Nom :</th>
            <td>{{ fournisseur.nom }}</td>
        </tr>
        <tr>
            <th>Numero de Téléphone :</th>
            <td>{{ fournisseur.numTel }}</td>
        </tr>
        </tbody>
    </table>
    <a href="{{ path('app_fournisseur_index') }}" class="btn btn-outline-secondary mt-3"><i class="fas fa-arrow-left"></i>
        Retour à la liste</a>


    {{ include('fournisseur/_delete_form.html.twig') }}

{% endblock %}
