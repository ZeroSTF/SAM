{% extends 'template.html.twig' %}

{% block body %}
    <div class="card col-md-12">
        <div class="card-header">
            <h3 class="card-title">Ajouter une nouvelle maintenance</h3>
        </div>
        <div class="card-body">
            {% set show_lines = true %}
            {{ include('maintenance/_form.html.twig', {'button_label': 'Envoyer'}) }}
        </div>
        <div class="card-footer">
            <a href="{{ path('app_maintenance_index') }}" class="btn btn-default">Retour à la liste</a>
        </div>
    </div>

{% endblock %}
